import { html } from '@lion/core';

export const currencyStyles = html`
  <style>
    /* Due to shadow DOM scoping: from this component through all children, apply a background-color */
    /*
    * {
      background-color: orange;
    }
*/
  </style>
`;
